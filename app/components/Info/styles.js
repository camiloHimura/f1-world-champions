import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  align-items: center;

  .infoIcon {
    font-size: 20px;
    color: var(--gray-2);
  }
`;

export const Text = styled.p`
  margin-left: var(--gPadding);
`;
